<?xml version="1.0" encoding="utf-8"?>
<network-security-config>

    <!-- Debug builds: разрешить HTTP для локальной разработки -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug mode: доверять пользовательским сертификатам (для HTTPS с self-signed cert) -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

    <!-- TODO: Production конфигурация (когда будет production сервер) -->
    <!-- <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">api.flotilla-server.com</domain>
        <pin-set expiration="2027-01-01">
            <pin digest="SHA-256">your-cert-hash-base64==</pin>
            <pin digest="SHA-256">backup-cert-hash-base64==</pin>
        </pin-set>
    </domain-config> -->

</network-security-config>